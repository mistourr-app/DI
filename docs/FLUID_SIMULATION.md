# Fluid Simulation — Design Document

> **📌 Статус:** 🚧 В работе — Фаза 1 ✅, Фаза 2: ядро ✅ (тюнинг/перф ⏸ бэклог), Фаза 3: 3.1+3.2 ✅, 3.3 ❌ отклонена, 3.4 🔶  
> **🏷️ Ветка:** `phase2/fluid-simulation`  
> **🎯 Цель:** Перенести физику толпы в Web Worker и сделать поведение монстров «как жидкость»: обтекание препятствий реками, расталкивание в толпе — при 1000+ агентов на экране без просадок FPS

---

## 📚 1. Цель и требования

### Источник требований
[GDD §2.1](../GAME_DESIGN_DOCUMENT.md): **до 1000 врагов на экране**, поведение моделируется как жидкость, визуализация — маленькие спрайты, объединённые в «поток». Техстек явно предписывает **Web Workers** для fluid simulation. Целевой FPS — 60.

### Что значит «жидкость» на практике
| Наблюдение | Механика |
|------------|----------|
| Толпа течёт вокруг препятствий «рукавами» | Движение строго вниз + обход блобов по касательной (уже есть, `moveDownStep`) + расталкивание соседями |
| Монстры не слипаются в одну точку | Сила separation по spatial hash |
| Плотная толпа «распирает» изнутри | Pressure от локальной плотности |
| Поток движется согласованно, без дёрганья | Viscosity / alignment к средней скорости соседей |
| Рукава держатся вместе, но не сливаются | Слабая cohesion |

> **📐 Решение по движению (as-built, 22.08.2026):** вместо steering к базе —
> движение **строго вниз** (`moveDownStep` в `fluidProtocol.ts`): пока вертикаль
> свободна — вниз с микро-шумом; упёрся в блоб — обход вбок полной скоростью
> в выбранную сторону (сторона запоминается до возобновления вертикали).
> База как цель не используется: `baseX/baseY` в `init` игнорируются,
> прибытие = `y > height`. Поток при этом обтекает блобы «как вода».

---

## 🔍 2. Текущее состояние

### Что заменяется
Движение сейчас целиком на main thread (`GameScene.updateEnemyMovement`):
- пул Image-спрайтов `'enemy'`, скорость в прямых свойствах `vx/vy`;
- steering к базе + шум ±0.01, клэмп `maxSpeed×1.05`;
- осевое скольжение по сетке `LevelGenerator.isBlocked` (CELL=16px), радиус-хитбокс `isBlockedBox` (4 угла, `hitR = max(4, enemySize·0.6)`);
- спасение застрявших (nudge вверх), боковые клэмпы поля;
- достижение зоны базы → `killAndHide()` → HP−1;
- спавн: раунд-робин по полноширинному входу над экраном (`zone.y − 32`).

При 1000+ агентов это уже работает в 60 FPS, но толпа ведёт себя как «рой независимых точек», а не жидкость, и вся физика лежит на главном потоке.

### Что переиспользуется как есть
| Актив | Как используется во fluid sim |
|-------|-------------------------------|
| Сетка коллизий `level.isBlocked` (Uint8Array 16px) | Копия передаётся в воркер; та же семантика осевого скольжения |
| Пул спрайтов `'enemy'` + Group | Рендер остаётся на main; позиции приходят из воркера |
| Спавн/деспавн, счётчики, HP базы, поп-ап ⚙ | Остаются на main; физике шлются только события add/remove |
| `GameConfig.enemies.fluid` | Константы сил уже заготовлены: `density 1.0, pressure 0.1, viscosity 0.01, separation 1.5, cohesion 0.5, alignment 0.3` — будут подключены |

### Что удаляется
- `src/game/utils/WorkerPool.ts` — task-based promise-pool: избыточен для непрерывной симуляции, `postMessage` без transfer list, сломанный выбор воркера (+ висящая TS-ошибка). Заменяется специализированным контроллером (§6).

### Вне рамок фичи
- ECS (`World.ts`, `CrowdAgent.ts`) — в рантайме движения не участвует; миграция агентов в bitecs — отдельное будущее решение.
- Рисование стихий, комбинации элементов — следующие задачи Фазы 2.

---

## 🏗 3. Архитектура

### 3.1. Ключевые решения

| # | Решение | Обоснование |
|---|---------|-------------|
| 1 | **Один выделенный worker** (не пул) | Симуляция непрерывна: состояние живёт в воркере постоянно. Пул нужен для разовых задач, не для 60 шагов/сек |
| 2 | **SoA-память**: `Float32Array px/py/vx/vy/radius`, ёмкость `MAX_AGENTS = 20000` (стресс-тесты); занятость через free-list (`Int32Array` стек) + `count` | Кэш-дружественно, ноль аллокаций в цикле, O(1) add/remove |
| 3 | **Transferable ping-pong буферов**, без SharedArrayBuffer | SAB требует COOP/COEP заголовков (усложняет dev/PWA/хостинг); копия 1000 агентов = ~16 KB/кадр — бесплатно |
| 4 | **Stepping управляет main**: каждый rAF шлёт `{type:'step', dt}` пока предыдущий кадр in-flight | Сим синхронна с рендером; внутри воркера фиксированный подшаг `1/60` c аккумулятором (макс. 3 подшага за кадр) |
| 5 | **Силы — SPH-lite/boids гибрид** поверх steering'а к базе | Дешевле полного SPH, даёт нужный «жидкий» вид; константы уже в конфиге |
| 6 | **Сетка коллизий копируется в воркер** при init и при каждой регенерации уровня | Коллизии считаются там же, где интеграция; main освобождён полностью |
| 7 | **Спавн/килл остаются на main** | Пул спрайтов, счётчики, HP — UI-состояние; воркеру шлются только `{add}/{remove}` |
| 8 | **Fallback main-thread** при отсутствии Worker | Игра остаётся играбельной везде; текущий код движения сохраняется как legacy-путь |

### 3.2. Поток данных за кадр

```
main thread                              FluidWorker
───────────                              ───────────
rAF:
  spawn?  ──{add id,x,y,vx,vy}──────────► free-list push
  kill?   ──{remove id}─────────────────► free-list pop
  если нет in-flight:
    ──{step, dt} + свободный out-буфер─►  fixed substeps ×N:
                                          1. rebuild spatial hash
                                          2. forces (steer+sep+press+visc)
                                          3. integrate (substep dt)
                                          4. collide (grid slide)
                                          5. collect arrived ids
    ◄─{frame, px,py[transfer], arrived}─
  sync sprites ← px/py (Map id→sprite)
  arrived → killAndHide, HP−−
```

### 3.3. Протокол сообщений

| Направление | type | Payload | Transfer |
|-------------|------|---------|----------|
| main→w | `init` | width, height, maxAgents, params(fluid) | — |
| main→w | `set_level` | cols, rows, cellSize, blocked(Uint8Array) | blocked ✅ |
| main→w | `add` | id, x, y, vx, vy, radius | — |
| main→w | `remove` | id | — |
| main→w | `step` | dt | out-буфер позиций ✅ |
| main→w | `params` | targetSpeed, enemyRadius, … | — |
| w→main | `ready` | — | — |
| w→main | `frame` | count, px(Float32Array), py(Float32Array), arrived(Int32Array) | все три ✅ |

Буферы позиций возвращаются main'ом воркеру вместе со следующим `step` (пул ≥2 буферов на стороне main).

### 3.4. Spatial hash
- Ячейка ≈ радиус взаимодействия (~24px), отдельная от коллизионной сетки 16px.
- Реализация linked-cell без аллокаций: `Int32Array head[hash]` + `Int32Array next[MAX_AGENTS]`.
- Поиск пар — только по своей ячейке + 8 соседних.

---

## ⚙️ 4. Алгоритм сил (на подшаг)

```
для каждого агента i:
  n      = соседи в радиусе R_sep (hash)
  dens_i = |n|
  
  flow   = moveDownStep(...)                            // база: строго вниз + обход блобов
  sep    = Σ_neighbors (p_i − p_j)/d²  · k_sep          // сильнейший вклад
  press  = dens_i > D_max ? (centroid_n − p_i)·k_press : 0
  visc   = (avg(v_n) − v_i) · k_visc                    // сглаживание потока
  coh    = (centroid_n − p_i) · k_coh                   // слабая

  a      = sep·k_separation + press·k_pressure 
           + visc·k_viscosity + coh·k_cohesion
  v_i   += a · substepDt
  clamp |v_i| ≤ maxSpeed·1.05
  p_i   += v_i · substepDt   // с коллизией сетки и границами поля
```

Константы `k_*` стартуют из `GameConfig.enemies.fluid`; тюнинг — Фаза 2. База движения —
`moveDownStep` (строго вниз, обход блобов, см. решение §1); силы жидкости модулируют
траекторию, а не заменяют цель «дойти до базы».

---

## 🧱 5. Коллизии и границы (в воркере)

- Семантика идентична текущей main-реализации: осевое скольжение `(nx,ny)` → `(nx,y)` → `(x,ny)` → стоп.
- Хитбокс: центр + 2 пробы вдоль вектора скорости (`±radius·0.6`). При визуальных артефактах поднимается до 4 углов как на main.
- Боковые границы поля: клэмп + демпфирование нормальной составляющей (как сейчас).
- Верхняя граница: клэмпов нет — агенты заходят сверху свободно (фикс застревания учтён).
- **Достижение базы:** `y > battlefieldHeight` → id уходит в `arrived[]` → main делает `killAndHide` и снимает HP. Логика базы полностью на main.

### 5.1. Антизастревание (as-built, 22.08.2026)

Строго вниз + «запомненная» сторона обхода создают ловушки. Механизмы решения
(все — внутри общей `moveDownStep`, паритет с fallback сохранён):

| Проблема | Решение |
|----------|---------|
| Агент коммитится в сторону без слива и стоит у стенки выемки | Разворот `avoid.value` при заблокированном горизонтальном ходе |
| Обе стороны закрыты (дно полости, плита у кромки) | **Всплытие**: подъём + спасательный полёт (`sweep` ≤ 1000 подшагов): уровень впереди свободен → летим вбок на текущей высоте; стена/край → всплываем; край поля → разворот |
| Полёт цепляет крышу углами или падает в псевдопросвет от шума | Полёт держит высоту; падение только после `hover ≥ 4` подшагов подтверждённой глубины (`+10px` проба вниз); hover сбрасывается при упоре |
| Всплытие срабатывает где попало / карабкается сквозь блобы | Порог устойчивой ловушки `TRAP_STREAK = 24` подшагов (в плотной толпе соседи блокируют бока на доли секунды — давка разбирается сама); подъём `climbStep` проверяет коллизию — сквозь скалу нельзя |
| Рассинхрон границы протокола (`cols*cell`) и внешнего клэмпа (worldW) | `CollisionField.widthPx` — реальная ширина поля едет из генератора через контроллер в воркер; граница движения везде одна |
| Фантомная полоса: сетка шире поля (`cols*cell` до +15px), невидимые препятствия цепляли хитбоксы у правого края | `blockedAt` игнорирует `x ≥ widthPx`; правый край симметричен левому |
| Спавн у кромки (входы на всю ширину) телепортом слипался в колонну на границе коридора → толпа запиралась и лезла вверх | Спавн клэмпится в коридор: `±14px` от краёв поля |
| Монстры, погребённые регенерацией (слайдеры генерации), «копали вверх» сквозь блобы | Телепорт на спавн вместо копания — и в воркере, и в fallback-пути |
| Экзотические петли любой природы | Фейлсейф воркера: нет прогресса вниз 1500 подшагов → возврат агента на спавн |

Плюс генерация: заливка недренируемых карманов + гарантия открытых входов
(см. [LEVEL_GENERATION.md §6.3.5](./LEVEL_GENERATION.md)).
Валидация: 20 seed'ов × 300 агентов — 100% прибытие; перф не изменился (~0.8 мс/шаг @1000).

---

## 📁 6. Интеграция и файлы

```
src/game/
├── fluid/                            # НОВОЕ
│   ├── fluidProtocol.ts              # Типы сообщений, константы, чистые функции сил
│   │                                 # (импортируются ОБЕИМИ сторонами — единая физика)
│   ├── FluidWorker.ts                # Воркер: substeps, hash, силы, коллизии
│   └── FluidSimulationController.ts  # Main-side: lifecycle, ping-pong, API для сцены
├── utils/
│   └── WorkerPool.ts                 # УДАЛИТЬ
└── generation/
    └── LevelGenerator.ts             # + маленькое API: getCollisionField() →
                                      #   {cols, rows, cellSize, blocked} (копия сетки;
                                      #   сама сетка иммутабельна после генерации)
scenes/GameScene.ts                   # Контроллер вместо updateEnemyMovement;
                                      # render-sync спрайтов; хуки spawn/kill
```

- Создание воркера: `new Worker(new URL('./FluidWorker.ts', import.meta.url), { type:'module' })` (нативная поддержка Vite 5).
- **Fallback:** `typeof Worker === 'undefined'` → `controller.isWorkerMode=false` → работает текущий `updateEnemyMovement`. Чистые функции сил импортируются обоими путями — расхождение физики исключено.
- Поп-ап ⚙: параметры спавна форвардятся в воркер (`params`); слайдеры сил — Фаза 3.

---

## 📋 7. План разработки

### 🟢 Фаза 1: Каркас и паритет движения (6–8 ч) — ✅ ВЫПОЛНЕНА
| Задача | Описание | Статус |
|--------|----------|--------|
| 1.1 | `fluidProtocol.ts`: типы, константы, чистая функция движения `moveDownStep` (вниз + обход по касательной) | ✅ |
| 1.2 | `FluidSimulationController`: lifecycle, ping-pong пул буферов, fallback-режим | ✅ |
| 1.3 | `FluidWorker.ts`: init/set_level/add/remove/step/frame, интеграция, grid-коллизии, arrived-list | ✅ |
| 1.4 | `LevelGenerator.getCollisionField()`; передача сетки при generate/regenerate | ✅ |
| 1.5 | GameScene: подключение контроллера, render-sync спрайтов, arrived → killAndHide/HP | ✅ |
| 1.6 | Удалить `WorkerPool.ts`; Vite module-worker + PWA-плагин бандлятся корректно, воркер в прекэше sw.js | ✅ |

**Критерий Ф1:** поведение визуально неотличимо от текущего (steering-паритет), физика вне main thread, FPS не ниже текущего, регенерация уровня обновляет воркер.

### 🟡 Фаза 2: Жидкость (8–10 ч) — 🔶 Ядро готово; тюнинг/перф отложены в бэклог
| Задача | Описание | Статус |
|--------|----------|--------|
| 2.1 | Spatial hash (`SpatialGrid` в `fluidProtocol.ts`: linked-cell, zero-alloc, покрытие зоны спавна сверху) | ✅ |
| 2.2 | Separation + pressure + viscosity + cohesion (`fluidDisplacement` там же); воркер: Pass A (moveDownStep) → hash → Pass B (силы с осевым скольжением); константы проводятся из `GameConfig.enemies.fluid` через `buildFluidParams` | ✅ |
| 2.3 | Тюнинг сессии на плотности 0.3/0.5 | ⏸ **Бэклог** (решение 22.08.2026: стартовые константы устраивают; после 3.1 тюнинг делается слайдерами без правок кода) |
| 2.4 | Перф-валидация: 1000+ агентов, замеры ms/step | ⏸ **Бэклог** (офлайн: ~0.8 мс/шаг @1000 при лимите 8; живая метрика доступна после 3.2) |

**As-built заметки Фазы 2 (22.08.2026):**
- Силы — чистая функция `fluidDisplacement` в протоколе; базовый поток не трогает,
  только модулирует (клэмп смещения ≤ 0.75×targetSpeed за подшаг).
- Фикс по итогам смоук-теста: при d² < SEP_MIN_D2=1 направление separation
  ненадёжно → случайный разворот вместо обнуления силы (иначе «стопки»
  совпавших агентов не разъезжаются).
- Стартовые масштабы: `SEP_SCALE=6, PRESS_SCALE=1.2, VISC_SCALE=20, COH_SCALE=0.15`,
  порог давления `DENSITY_MAX=8` соседей.
- Смоук-тест (зеркало логики воркера на чистых функциях, поле 400×700 с двумя
  блобами, 1000 агентов): прибытие на базу работает, NaN нет, min дистанция
  между агентами ~7 px, 0.8 мс/шаг.

**Критерий Ф2:** рукава вокруг блобов, отсутствие стопок наложенных спрайтов, 60 FPS desktop @1000 агентов.

### 🔵 Фаза 3: Полировка (3–5 ч) — 🔶 В работе (3.1, 3.2 готовы)
| Задача | Описание | Статус |
|--------|----------|--------|
| 3.1 | Дебаг-слайдеры separation/pressure/viscosity/cohesion в поп-апе ⚙ (мутация `GameConfig.enemies.fluid` → `params` в воркер на лету) + кнопка «Сбросить силы» | ✅ |
| 3.2 | Метрика `SimW: X.Xмс` (EMA по кадрам) в дебаг-панели; тег W/M = воркер/fallback | ✅ |
| 3.3 | ~~HUD-кнопка ускорения ×1/×2/×4~~ — ❌ ОТКЛОНЕНА (решение 22.08.2026: не делаем) | ❌ |
| 3.4 | Проверка на среднем мобильном устройстве; документ → as-built. Тест-конфигурация: лимит на экране снят до ёмкости физики (`MAX_AGENTS = 20000`), уровень до 100k (шаг слайдера растёт с величиной); дефолты не тронуты | 🔶 Infinix Note 30i проверен вживую; стресс-тест 100k — в процессе |

### 📊 Итого: **17–23 часа**

---

## ✅ 8. Критерии приёмки фичи

- [ ] 1000+ монстров @ 60 FPS (desktop Chrome); средний мобильный ≥ 30 FPS
- [ ] Main thread не считает физику: только синхронизация позиций спрайтов (<1 мс/кадр)
- [ ] Шаг воркера ≤ 8 мс на 1000 агентов (без аллокаций в steady-state)
- [ ] Толпа обтекает блобы «реками»; в плотных местах — расталкивание; нет взаимных наложений
- [ ] Лабиринт непроходим (семантика сетки сохранена 1-в-1)
- [ ] Достижение базы убивает агента и снимает HP (через arrived-list)
- [ ] Регенерация уровня / кнопка «Сгенерировать уровень» корректно обновляют воркер
- [ ] Режим без Worker (fallback) играбелен

---

## ⚠️ 9. Риски и решения

| Риск | Решение |
|------|---------|
| Задержка позиций на 1 кадр | Приемлема визуально; при необходимости — интерполяция позже |
| GC pressure от постинга буферов | Transferables + переиспользуемый пул буферов; ноль аллокаций в воркере |
| Дублирование физики (worker vs fallback) | Единый модуль чистых функций, импортируется обеими сторонами |
| Бандлинг worker + vite-plugin-pwa | Проверяется первой же задачей Фазы 1 (1.6), до углубления |
| Расхождение хитбоксов (main 4 угла vs worker пробы) | Стартуем с проб; артефакты → поднимаем до 4 углов |
| «Вязкая» толпа перекрывает проход < passageWidth | Pressure-порог D_max настраивается; passageWidth уже гарантирует мин. просвет генератором |

---

## 📝 10. История изменений

| Дата | Изменение | Автор |
|------|-----------|-------|
| 22.08.2026 | **Фикс потери прибытий**: при 2+ подшагах за кадр (30fps мобильные) `arrivedCount = integrate()` перезаписывал счётчик — спрайты зависали у базы, HP недоотчитывался. Теперь накопление за все подшаги. Ёмкость `MAX_AGENTS` 4096→20000; переработка id в контроллере (после 4096 суммарных спавнов физика переставала принимать агентов). Лимит экрана до 20000, шаг слайдера уровня адаптивный (до 100k). 3.3 (fast-forward) отклонена | @ox-alpha |
| 22.08.2026 | HiDPI-рендер (канвас в физических px, `uiScale.ts`: UI_SCALE/TEXT_BOOST/fontPx/padPx); TRAP_STREAK 6→24 (давка в плотной толпе без карабканья); фикс фантомной полосы справа (`blockedAt` × widthPx); клэмп спавна в коридор ±14px; погребённые регенерацией телепортируются на спавн; плотность слайдера до 2.0; дефолты генерации 1.1/×0.3; CI ускорен (~2 мин, skip puppeteer) | @ox-alpha |
| 22.08.2026 | Фикс «монстры плывут вверх сквозь блобы у правого края»: подъём с коллизией (`climbStep`), порог всплытия `TRAP_STREAK=6`, `CollisionField.widthPx` (реальная ширина поля вместо cols*cell). Генерация: эрозия свободного места 3×3 (проходы ≥48px), резка кластеров >12% площади крестом коридоров. Валидация: 20 seed'ов × 300 = 100%, перф 0.59 мс/шаг | @ox-alpha |
| 22.08.2026 | Фаза 3: 3.1 слайдеры сил в поп-апе ⚙ (+сброс), 3.2 метрика SimW мс в дебаг-панели; 2.3/2.4 отложены в бэклог (стартовые константы устраивают) | @ox-alpha |
| 22.08.2026 | Фикс «монстры не выбираются из глубоких выемок»: разворот стороны обхода, всплытие + спасательный полёт, единая EDGE_MARGIN=10, клэмп X при падении, фейлсейф возврата на спавн (§5.1); в генераторе — заливка недренируемых карманов + гарантия открытых входов; валидация 20 seed'ов × 300 агентов = 100% прибытие | @ox-alpha |
| 22.08.2026 | Фаза 2 (2.1, 2.2): spatial hash + силы жидкости в воркере, проводка констант из GameConfig; смоук-тест физики вне браузера, фикс «стопок» (SEP_MIN_D2), стартовые масштабы сил | @ox-alpha |
| 22.08.2026 | Фаза 1 выполнена: воркер + контроллер + протокол, `moveDownStep` (вниз с обходом блобов — решение зафиксировано в §1), WorkerPool удалён, бандл Vite+PWA проверен; док приведён к as-built | @ox-alpha |
| 22.08.2026 | Создан документ: архитектура, протокол, план фаз | @ox-alpha |
| 22.08.2026 | Разбор референса (Sir, We Have an Orc Problem): ghost-preview **отклонён** (у нас другая механика); добавлена задача HUD-кнопки ускорения симуляции (3.3) | @user + @ox-alpha |

---

## 🔗 11. Ссылки
- [Game Design Document](../GAME_DESIGN_DOCUMENT.md) — §2.1 Поведение врагов
- [LEVEL_GENERATION.md](../LEVEL_GENERATION.md) — сетка коллизий, переиспользуемая воркером
- **Референс:** [Sir, We Have an Orc Problem](https://store.steampowered.com/app/4594150/Sir_We_Have_an_Orc_Problem/) — толпа орков течёт к базе как жидкость; физика там на GPU (100k юнитов, десктопный Steam), наш масштаб — CPU worker в браузере
