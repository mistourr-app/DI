# Element System — Design & As-Built Documentation

> **📌 Статус:** ✅ Итерация 1 (ядро рисования); ✅ Итерация 2 (Земля-барьер); ✅ Итерация 3 (прочность Земли, per-cell HP); ✅ Итерация 4 (базовые эффекты Огонь/Вода/Воздух) — **фича завершена**
> **🏷️ Ветка:** `phase2/element-system`
> **🎯 Результат:** Игрок применяет стихии к полю боя: выбирает алтарь в зоне базы, ведёт пальцем — рисуется линия применения (толщина настраиваемая). Стихии тратят ману, которая копится по алтарям независимо и наполняется от убийств монстров. **Земля — непроходимый барьер** из ячеек с прочностью на ячейку. **Огонь/Вода/Воздух — базовые эффекты** (горение с ограниченным цепным поджогом, замедление, отброс). Комбинации стихий — бэклог.

---

## 📚 1. Терминология и обзор механики

| Термин | Описание |
|--------|----------|
| **Алтарь стихии** | Круглая иконка в зоне базы (Огонь, Вода, Земля, Воздух). Тап по алтарю **выбирает** (армит) стихию для рисования |
| **Мана алтаря** | Независимый запас каждого алтаря, с собственной ёмкостью и прогресс-баром. Источник — убийства монстров |
| **Рисование стихии** | После выбора алтаря палец ведётся по полю боя: рисуется линия (толщина = `radius`), списывается мана. **На препятствиях стихия не рисуется** |
| **Сегмент** | Одна нарисованная линия (штрих). У Воды/Огня/Воздуха исчезает со временем; у Земли — стойкий барьер (Итерация 2) |
| **Главная стихия (сила бога)** | Существующая механика (ГДД 2.5) — **НЕ трогаем**: полный заряд от молний, применение по тапу |

### Модель маны (as-согласовано, 10.09.2026)
- **Источник маны = убийства монстров.** Каждый убитый монстр даёт **каждому** алтарю свою порцию `gainPerKill` (может отличаться у разных алтарей).
- У каждого алтаря своя **ёмкость** `capacity` (независимая). Полный бар → мана больше не копится.
- Использование списывает `costPerUse` за **каждый юнит длины** (юнит = `elementStrokeUnit`, свой `costPerUse` у каждого алтаря); списание поюнитно в реальном времени. Применять можно при любом запасе, достаточном хотя бы на один юнит.
- Убийства учитываются только боевые (молния/супер сила). Монстры, дошедшие до базы (сняли HP и исчезли), ману **не** дают.
- Все параметры (`gainPerKill`, `capacity`, `costPerUse`, `radius`, `earth.bitesPerCell`) — конфигурируемые, прокачиваются в будущем.

### Поведение стихий (финальное)
| Стихия | Поведение |
|--------|-----------|
| **Вода** | Рисуется слой, fade по `duration`; **эффект — замедление** (Итерация 4): монстры в зоне ползут `slowFactor`-скорости, статус **Мокрый** (голубой) держится `wetDuration` (5 с) после выхода — замедление сохраняется, пока мокрый. Тушение — с комбинациями (позже) |
| **Огонь** | Рисуется слой, fade по `duration`; **эффект — горение** (Итерация 4): статус **Горение** (жёлтый) — пока в зоне; вошедший в огонь поджигается, горит `burnDuration` сек и умирает; при смерти — минимальный поджог соседей (шанс `spreadChance`, лимит `maxIgnitePerDeath`) |
| **Земля** | **Барьер из ячеек**: непроходим; монстры касаются → атакуют и гибнут, грызя ячейки (прочность `bitesPerCell`, кусок `biteRadius`); через дыры проходят |
| **Воздух** | Рисуется слой, fade по `duration`; **эффект — отброс с инерцией** (Итерация 4): монстров сдувает в направлении жеста рисования (сила `pushStrength`); после выхода они сохраняют инерцию (дрейф затухает постепенно), статус **Сдутый** (бледно-синий `#B4C2F0`) держится `airDuration` (5 с) |

### Комбинации (позже)
Список комбинаций и свойства получающихся сущностей добавляются позже. Очевидный пример: **Огонь + Вода = Пар**. Вне рамок текущих итераций.

### Эффекты стихий (Итерация 4, as-built)
Базовые эффекты Огонь/Вода/Воздух — **без комбинаций**. Строятся на **статусах монстров** (§PERFORMANCE.md 3.7): перекраска (tint) только при смене статуса, никаких доп. спрайтов/draw calls.

**Принцип — от длительности эффекта:** зона (штрих) живёт `duration` секунд и **пока существует — работает** (огонь жжёт, вода замедляет, ветер сдувает вошедших). После выхода из зоны статус **держится ещё** (`wetDuration`/`airDuration`, дефолт 5 с) — монстры «высыхают» и «теряют инерцию» по времени. `duration` Огонь/Вода/Воздух = **5 с**, `wetDuration`/`airDuration` = **5 с** (общие ручки в поп-апе, конфиг остаётся per-element для будущей раздельной прокачки).

| Стихия | Эффект | Где применяется | Параметры (`GameConfig.elements`) |
|--------|--------|-----------------|-----------------------------------|
| **Огонь** | **Горение**: вошёл в зону → поджог, горит `burnDuration` сек → смерть; цепной поджог при смерти — строго ОДНОГО соседа | main (`ElementEffectSystem` → `killEnemy`) | `burnDuration` (2.0 с), `burnRadius` (40), `spreadChance` (0.2), `maxIgnitePerDeath` (**1**, прокачиваемый) |
| **Вода** | **Замедление** × `slowFactor`; статус **Мокрый** (голубой) и замедление держатся `wetDuration` после выхода | воркер (`waterSlowFactor`/`wetDuration` в `FluidParams`) | `slowFactor` (0.5), `wetDuration` (5 с) |
| **Воздух** | **Отброс с инерцией** в направлении жеста; статус **Сдутый** (`#B4C2F0`) держится `airDuration` после выхода | воркер (`airPushStrength`/`airDuration` в `FluidParams`) | `pushStrength` (1.5 px/подшаг), `airDuration` (5 с) |

**Инерция воздуха:** вместо мгновенного толчка — дрейф-скорость с экспоненциальным затуханием. В зоне дрейф стремится к `ветер × pushStrength` (отклик `AIR_RESPONSE=0.35`), вне зоны затухает (`AIR_DAMP=0.97`/подшаг ≈ 0.16/сек) и **продолжает сносить** — монстр разгоняется и **плавно замедляется до дефолтной скорости ~2.5 с** после выхода из зоны. Параллельно в `FluidWorker` (массивы `driftX/driftY`) и в fallback (`enemy.driftX/driftY`). Мокрый-лингер — `wetRemain[s]` в воркере (декрет по `SUBSTEP_DT`) и `enemy.wetRemain` в fallback.

**Цвета статусов (§3.7, tint × белая база):** дефолт `#ED0000`, горение `#FFE206`, мокрый `#00D9FF`, сдутый `#B4C2F0`. Приоритет: горение → сдутый → мокрый → дефолт. Статусы держатся, пока действует эффект.

**Ограничение цепного горения:** при смерти горящего поджигается сосед в `burnRadius` — **не более `maxIgnitePerDeath` = 1** («огонь передаётся от одного монстра только к одному»; параметр прокачиваемый), каждый кандидат с шансом `spreadChance`. Ожидаемое число поджигов за смерть = `spreadChance × maxIgnitePerDeath` < 1 (0.2×1 = 0.2): цепь субкритична, затухает. Цепно подожжённые (`chainIgnited`) горят до дедлайна даже вне зоны.

**Зоны-эффекты = сеточный оверлей (паттерн Земли):** штрих растеризуется в ячейки коллизионной сетки (`cellsWithinCircle`), в воркер уходит `set_effects { effect, cells, value, dirX?, dirY? }` (вода/воздух), в main остаются счётчики ячеек (`fireCount/waterCount/airCount`) с **refcount** — перекрывающиеся штрихи гаснут, только когда ячейку покинули все. О(1) lookup на агента, ноль аллокаций в кадре.

**Физика эффектов в воркере** (`integrate`): вода — `effectiveSpeed = targetSpeed × waterSlowFactor`; воздух — `px/vx += airDir × airPushStrength` с осевым скольжением по коллизиям. Fallback-путь (`updateEnemyMovement`) — паритет через `ElementEffectSystem.waterAt/airAt`.

**Текстура монстров — нейтральная (серая):** цвет задаётся tint'ом (§3.7). Дефолт — красный `#ED0000`, горение — жёлтый `#FFE206`, мокрый — голубой `#00D9FF`.

---

## 🔌 2. Параметры баланса (конфигурируемые, будущая прокачка)

Каждой стихии в `GameConfig.elements` (кроме уже имеющихся `color/damage/duration/radius/cost`):

| Параметр | Описание | Прокачка |
|----------|----------|----------|
| `capacity` | Ёмкость маны алтаря (потолок накопления) | Да |
| `gainPerKill` | Порция маны алтарю за одного убитого монстра | Да |
| `costPerUse` | Цена за **один юнит длины** штриха | Да |
| `radius` | Толщина линии (радиус зоны применения), css-px | Да |
| `elementStrokeUnit` (общий) | Длина юнита штриха, css-px (дефолт 50) | Да |
| `earth.biteRadius` | Радиус «куска», который грызёт монстр за укус, css-px (24) | Да |
| `earth.bitesPerCell` | Прочность ячейки Земли: укусов держит (5); **прокачиваемый** | Да |

> **Дефолты (баланс):** `gainPerKill = 0.3`, `capacity = 20`, `costPerUse = 7`, `radius = 25` для всех четырёх стихий.

> Модель стоимости: линия наносится **сразу** (без предпросмотра), мана списывается **поюнитно в реальном времени**: за `N` нарисованных юнитов (каждый = `elementStrokeUnit`) платится `N × costPerUse`. Бюджет штриха = `floor(mana / costPerUse)` юнитов — дальше него линия не рисуется (обрыв в точке исчерпания маны). Неполный хвостовой юнит доплачивается при завершении (`ceil`).

### Персистентность настроек (TuningStore)
- Все параметры, настраиваемые в поп-апе ⚙ (спавн, генерация, сила бога, стихии, прочность Земли), сохраняются в **localStorage** (`fluid-crowd-defense.tuning.v1`) при каждом изменении (`updatePopupValues`/`setLevel`).
- Восстанавливаются в `create()` (`applyStoredTuning`) — **не сбрасываются** ни при смене уровня/перезапуске сцены, ни при перезагрузке страницы.
- `capacity` и `elementStrokeUnit` в поп-апе не настраиваются и не персистятся.
- Через прокачку (`GameConfig.elements.*`) экономику/эффекты стихий меняет экран прокачки (см. [PROGRESSION.md](./PROGRESSION.md)) — оба источника сосуществуют: поп-ап перезаписывает в TuningStore, прокачка мутирует GameConfig напрямую.
- **Полный сброс:** `TuningStore.clear()` (добавлен 20.09.2026) стирает сохранённый тюнинг; используется кнопкой «Сбросить прогресс» на экране прокачки (fresh start — чтобы `applyStoredTuning` не вернул старые значения при рестарте).

---

## 📁 3. Реализация: структура файлов

```
src/
├── game/
│   ├── config/
│   │   ├── GameConfig.ts        # elements (capacity/gainPerKill/costPerUse/radius),
│   │   │                        # elementStrokeUnit, earth (biteRadius/bitesPerCell), godPower
│   │   └── uiScale.ts           # UI_SCALE/fontPx/padPx (глобальное масштабирование)
│   ├── element/
│   │   ├── ElementManaSystem.ts # Чистый TS: per-altar мана, арм-состояние, spend
│   │   ├── ElementDrawer.ts     # Жест рисования (тап-алтарь + слайд), бюджет маны
│   │   ├── ElementAltarIcon.ts  # Алтарь: заливка маны снизу вверх, затемнение по canUse
│   │   ├── EarthBarrierSystem.ts# Земля-барьер: ячейки + cellHp, biteCellsAround, рендер
│   │   └── earthCells.ts        # Чистые хелперы ячеек (cellsWithinCircle/cellAt/biteCells)
│   ├── fluid/
│   │   ├── fluidProtocol.ts     # + earth-оверлей, set_earth, канал attacks
│   │   ├── FluidWorker.ts       # Earth-оверлей + «стоп и атака» при контакте
│   │   └── FluidSimulationController.ts # setEarth, attacks-буфер в ping-pong
│   └── save/
│       └── TuningStore.ts       # Персистентность параметров поп-апа (localStorage)
├── scenes/
│   └── GameScene.ts             # Интеграция: ввод, gainFromKills, алтари, атаки земли
└── docs/
    └── ELEMENT_SYSTEM.md        # Этот документ
```

Протокол Земли: `set_earth { cells: Int32Array, value: 0|1 }` (добавить/убрать пакетом, transferable) + канал `attacks` (id атакующих) в `FrameMsg`. `moveDownStep` не менялся — единая физика (паритет worker/fallback).

---

## 🔧 4. API

### ElementManaSystem (чистый TS, тестируемый без Phaser)

```ts
const mana = new ElementManaSystem(GameConfig.elements);

mana.manaOf('water');         // текущий запас маны алтаря
mana.capacityOf('water');     // ёмкость алтаря
mana.progress('water');       // 0..1 заполнение бара алтаря
mana.gainFromKills(n);        // убито n монстров -> порция каждому алтарю (cap)
mana.canUse('water');         // хватает ли маны на costPerUse
mana.use('water');            // списание costPerUse (если canUse)
mana.spend('water', amount);  // списание произвольной суммы (поюнитно)
mana.reset();                 // сброс всех запасов (новый уровень)
mana.onBalanceChanged();      // клэмп после изменения параметров

// Арм-состояние (выбранный алтарь для рисования)
mana.armed;                   // ElementType | null
mana.arm('water');            // выбрать стихию (true — можно рисовать)
mana.disarm();                // снять выбор
```

Инварианты: мана не превышает `capacity` (cap при накоплении); `use()`/`spend()` при нехватке ничего не списывают и возвращают `false`; `armed` не может указывать на стихию без доступной маны.

### ElementDrawer

```ts
new ElementDrawer(scene, isBlocked, onEarthStroke);
drawer.beginPotential(x, y, key, cfg, maxPaintedLength); // pointerdown
drawer.onMove(x, y);                                      // pointermove
drawer.end(): 'draw' | 'tap' | 'none';                    // pointerup
drawer.paintedLength / drawer.totalLength;                // для доплаты (settle)
drawer.cancel(); drawer.clearPersistent(); drawer.destroy();
```

- `isBlocked(x, y)` — коллизия в мировых координатах (препятствия не рисуются).
- `onEarthStroke(points)` — Земля-штрих отдаёт путь (сцена растеризует в барьер).
- `beginPotential` получает бюджет маны (`floor(mana/costPerUse)` юнитов) — дальше него линия не рисуется.
- Обработка жеста: `pointerdown` → потенциальный штрих (если алтарь выбран) либо молния; движение выше порога → рисование; `pointerup` без движения → `'tap'` (молния + disarm), без нарисованного → `'none'` (мана не тратится, выбор сохраняется).

---

## ⚙️ 5. Детали реализации (as-built)

### 5.1. Развод жестов «тап vs слайд»
- Тап по полю боя = молния/супер сила (существующая логика, без изменений).
- Тап по алтарю в зоне базы выбирает стихию (арм).
- После арма движение пальца по полю боя выше `controls.touch.swipeThreshold` считается рисованием; `pointerup` без движения — тап (молния) и disarm.
- **Выбор сбрасывается только тапом по полю (молния) или выбором другого алтаря.** После штриха (в т.ч. `none` — только по препятствиям) выбор **сохраняется**: можно рисовать куски в разных местах без повторного выбора.
- Поп-ап настроек по-прежнему перехватывает тапы (`settingsOpen` guard).

### 5.2. Препятствия не рисуются
- Рисование опрашивает коллизионную сетку уровня (`isBlocked` в мировых координатах) для каждой точки пути.
- **Начало над препятствием** — точка не рисуется; рисование стартует, когда палец выходит на свободное место.
- **Заход на препятствие во время штриха** — жест НЕ сбрасывается: на препятствии пауза, после выхода линия продолжается с нового сегмента.
- **Полностью над препятствием** — `end()` возвращает `none`: ничего не рисуется, мана не тратится, выбор снимается.

### 5.3. Мана: цена за длину, списание в реальном времени
- Линия наносится на поле **сразу** (без предпросмотра и последующего списания за штрих).
- `costPerUse` — цена за **юнит длины** (`GameConfig.elementStrokeUnit`, дефолт 50 css-px). За `N` юнитов — `N × costPerUse` (10 юнитов при цене 1 = 10 маны).
- Списание **инкрементальное**: на каждом `pointermove` пройденные юниты списываются (`spend`), бар убывает сразу (`chargeStrokeUnits`).
- **Бюджет**: на `beginPotential` = `floor(mana / costPerUse)` юнитов; дальше него drawer не рисует (стоп в точке исчерпания маны). Неполный хвостовой юнит доплачивается на `pointerup` (`settleStrokeUnits`, `ceil`).
- Препятствия не рисуются и не в счёт; полностью над препятствием — 0 маны, `end()` = `none`.

### 5.4. Визуализация слоя
- Каждый штрих — `Graphics` в цвет стихии, толщина `radius * UI_SCALE`, наносится **сразу** (без предпросмотра).
- Вода/Огонь/Воздух: слой плавно исчезает за `duration` мс (с момента отпускания).
- Земля (Итерация 2): превращается в барьер из ячеек (см. §5.8), отдельная Graphics.

### 5.5. Накопление маны
- `ElementManaSystem.gainFromKills(n)` вызывается в точках боевых убийств: `castLightning` и `castSuperAttack` (после подсчёта жертв).
- Рестарт уровня/зачистка пула (`restartLevel`) ману не начисляет.
- Сброс запасов — при смене уровня (`reset()`).

### 5.6. Алтари (UI)
- Алтари рисуются в `createElements()` (`ElementAltarIcon`): чёрный круг, заливка маны **цветом стихии снизу вверх** (механизм как у `GodPowerIcon`), кольцо-подсветка выбранного.
- **Доступность:** иконка и кольцо **яркие**, когда стихия выбрана ИЛИ маны хватает на штрих (`canUse`); **затемнены** (alpha 0.35), когда маны не хватает — независимо от степени заполнения бара. Прогресс-бар всегда виден.
- Полный алтарь — лёгкая пульсация заливки; выбранный — белое кольцо, увеличение, пульс.
- Перерисовка — событийная (`redraw`), не каждый кадр (паттерн `GodPowerIcon`).

### 5.7. Баланс в поп-апе настроек (⚙)
- **«Мана за убийство»** — `gainPerKill`, диапазон 0.1–2, шаг 0.1. Одна строка, применяет одинаковое значение **всем четырём алтарям** (конфиг остаётся per-altar для будущей прокачки).
- **«Стоимость огня/воды/земли/воздуха»** — `costPerUse` каждого алтаря отдельно, диапазон 1–100, шаг 1 (цена за юнит `elementStrokeUnit` px). После изменения вызывается `onBalanceChanged()` (снимает выбор, если стихия стала недоступна).
- **«Радиус штриха»** — общий `radius`, диапазон 10–100, шаг 5, применяется ко всем алтарям; действует со следующего штриха (читается на `beginPotential`).
- **«Убийств на заряд бога»** — `superChargeRequired`, 10–500, шаг 10 (выше = медленнее заряд); после изменения `godPower.onBalanceChanged()` + `godIcon.redraw()`. Дефолт 80 — заполнение в 2 раза медленнее прежнего (было 40).
- **«Прочность земли (укусов)»** — `earth.bitesPerCell`, 1–100, шаг 1 (выше = землю грызут медленнее); применяется к новым штрихам сразу (живое значение). Прокачиваемый атрибут.
- Панель: компактные строки (`rowHeight = fontPx(28)`), высота считается по числу строк (`rowCount = 18`), влезает на малых экранах.

### 5.8. Земля-барьер (Итерации 2-3, as-built)
- Земля = **набор ячеек коллизионной сетки** (`EarthBarrierSystem`) с **прочностью НА ЯЧЕЙКУ** (`bitesPerCell` укусов) — глобальной прочности нет, монстры прогрызаются.
- **Растеризация штриха:** путь из `ElementDrawer` (точки ± `radius`) → ячейки (`cellsWithinCircle`) → `addStroke`: ячейки получают HP = `bitesPerCell` (живое значение из конфига), `set_earth add` воркеру + перерисовка.
- **Визуал:** каждая ячейка — перекрывающийся круг `r ≈ 0.72·клетки`; **темнеет с потерей HP** (`k = hp / max`, плавный лерп к тёмной базе), исчезает при 0. Прогрызенные ячейки = дыры.
- **Воркер:** оверлей `earth` в `CollisionField`; `blockedAt` = препятствие ‖ земля; `earthAt`/`isBoxEarth` — контакт. В шаге до `moveDownStep`: хитбокс касается земли → `vx=vy=0`, id в канал `attacks` (`FrameMsg`). `moveDownStep` не менялся — паритет с fallback.
- **Атака (main):** id из `attacks` → `killEnemy` (БЕЗ маны и заряда бога) + `biteCellsAround(x, y, biteRadius·UI_SCALE)`: ячейки в радиусе теряют **1 HP** (чистая `biteCells`); при 0 → `set_earth remove` + перерисовка. Fallback-путь — тот же контакт в `updateEnemyMovement`.
- **Баланс:** `GameConfig.earth.biteRadius = 24` (ширина «куска» за укус), `bitesPerCell = 5` — **прокачиваемый атрибут** (выше = землю грызут медленнее; ручка в поп-апе «Прочность земли (укусов)» 1–100, экономика прокачки — будущее).
- **Сброс:** `set_level` обнуляет оверлей воркера; `generateLevel` пересоздаёт `EarthBarrierSystem`.
- **Земля поверх монстров:** накрытый землёй монстр **гибнет, грызя ячейки** (паритет worker/fallback). Телепорт на спавн (`respawnToSpawn`) срабатывает ТОЛЬКО от препятствий уровня (`blockedByLevel`), а не от земли — иначе накрытые землёй монстры «выныривали» бы сверху.
- Поведение: скорость потери ячеек = `монстры/сек ÷ bitesPerCell` → масштабируется под плотность; на высоких уровнях прочность поднимается прокачкой.

---

## ✅ 6. Критерии приёмки (Итерация 1)

- [x] Тап по полю боя по-прежнему молния/супер сила (без регрессий)
- [x] Тап по алтарю выбирает стихию; рисуется линия толщиной `radius` в цвет стихии
- [x] Тап без движения при выбранной стихии = молния + disarm
- [x] Вода/Огонь/Воздух fade по `duration`; Земля — стойкий слой
- [x] На препятствиях стихия не рисуется: старт над препятствием — рисует после выхода; заход на препятствие — пауза и продолжение; полностью над препятствием — ничего, 0 маны
- [x] Мана пропорциональна нарисованной длине (свободный штрих = costPerUse; частично над препятствием — меньше; полностью — 0)
- [x] Мана: убийства наполняют каждый алтарь на свою порцию; бар не переполняется; **списание по длине в реальном времени** (юнит = `elementStrokeUnit`, цена `costPerUse` за юнит); при нехватке маны линия обрывается на границе юнита
- [x] Сброс маны при смене уровня; монстры у базы ману не дают
- [x] `tsc --noEmit`, `vite build`, jest-тесты `ElementManaSystem` зелёные
- [x] headless-смоук через `window.__di` без console/pageerror

## ✅ 6.1. Критерии приёмки (Итерация 2, Земля-барьер)

- [x] Нарисованная Земля непроходима (ячейки в коллизиях воркера + main-оверлей); **накрытые землёй монстры гибнут, грызя ячейки** (телепорт на спавн — только от препятствий уровня, не от земли)
- [x] Монстр, касающийся земли, атакует и гибнет; за укус ячейки в `biteRadius` теряют HP (БЕЗ маны/заряда бога)
- [x] **Прочность на ячейку**: ячейка темнеет по HP и исчезает после `bitesPerCell` укусов; скорость = `монстры/сек ÷ bitesPerCell`
- [x] Через прогрызенную дыру монстры проходят дальше (до базы)
- [x] `set_level` сбрасывает землю; `EarthBarrierSystem` пересоздаётся при регенерации
- [x] Fallback-путь (без Worker) ведёт себя так же
- [x] jest на чистых хелперах (`earthCells`: `biteCells` в т.ч.); tsc/build/headless-смоук зелёные

## ✅ 6.2. Критерии приёмки (Итерация 4, базовые эффекты стихий)

- [x] Огонь: монстр вошёл в зону — **Горение** (жёлтый tint), горит `burnDuration` сек → `killEnemy`; ушёл из зоны — пламя гаснет (эффект живёт, пока существует зона); при смерти — минимальный цепной поджог (≤ `maxIgnitePerDeath`, шанс `spreadChance`), цепь субкритична (0.2×2=0.4)
- [x] Вода: монстр в зоне — замедление `slowFactor` (воркер), статус **Мокрый** (голубой) и замедление держатся `wetDuration` (5 с) после выхода (лингер по времени); тушения нет (комбинации позже)
- [x] Воздух: отброс вошедших в зону в направлении жеста, сила `pushStrength`; **инерция** — дрейф затухает постепенно (0.92/подшаг), статус **Сдутый** (`#B4C2F0`) держится `airDuration` (5 с); воркер + fallback-паритет
- [x] Цвета: дефолт `#ED0000`, горение `#FFE206`, мокрый `#00D9FF`, сдутый `#B4C2F0`; приоритет горение → сдутый → мокрый → дефолт; статусы держатся, пока действует эффект
- [x] Длительность зоны Огонь/Вода/Воздух = 5 с и статуса (вода/воздух) = 5 с — две ручки в поп-апе (конфиг per-element для будущей раздельной прокачки)
- [x] Монстры рисуются ПОВЕРХ стихий (depth 850 > штрихов 800 и земли 700); вспышки бога (900) и UI — выше
- [x] Эффекты в воркере — О(1) lookup по сетке (`waterAt/airAt`), ноль аллокаций; тик статусов на main — с троттлингом ~10 Гц, `setTint` только при смене статуса
- [x] Текстура монстра нейтральная (серая), дефолтный красный tint; перекраска статусов корректа
- [x] Зоны гаснут по `duration` (refcount при перекрытиях); сброс при смене уровня (`set_level` обнуляет оверлеи воркера)
- [x] jest на чистом ядре (`effectsCore`: `applyStatus`/`igniteNeighbors`/`tintFor`) и `fluidProtocol` (`waterAt`/`airAt`); tsc/build/headless-смоук зелёные

---

## 🗺️ 7. План реализации

### Итерация 1: Ядро рисования — ✅ ЗАВЕРШЕНА (10.09.2026)
1. `ElementManaSystem` (чистый TS) + jest-тесты ✅
2. Конфиг: `capacity/gainPerKill/costPerUse` для каждой стихии ✅
3. `ElementDrawer` (жест + слой) ✅
4. Интеграция `GameScene`: ввод, `gainFromKills`, алтари с барами маны ✅
5. Верификация (tsc/build/jest/headless) ✅

### Итерация 2: Земля-барьер — ✅ ЗАВЕРШЕНА (10.09.2026)
1. Протокол: `set_earth` (добавить/убрать ячейки), канал `attacks` в `FrameMsg` ✅
2. Воркер: earth-оверлей в `CollisionField`; контакт с землёй → стоп + атака (без изменения `moveDownStep`) ✅
3. `EarthBarrierSystem`: ячейки + растеризация штриха; UI прочности — ❌ НЕ делаем (механика «куски») ✅
4. Верификация (tsc/jest/build/headless) ✅

### Итерация 3: Прочность Земли (per-cell HP) — ✅ ЗАВЕРШЕНА (10.09.2026)
1. `GameConfig.earth.bitesPerCell = 5` — прокачиваемый атрибут ✅
2. Чистая `biteCells` (earthCells) + jest ✅
3. `EarthBarrierSystem`: `cellHp`/`cellMax`, `biteCellsAround`, темнение по HP ✅
4. Атаки (воркер + fallback) → `biteCellsAround`; поп-ап «Прочность земли (укусов)» + персист (TuningStore) ✅
5. Верификация: M=50 держит (112/114), M=1 пропускает (дыры + HP базы падает) ✅

### Итерация 4: Базовые эффекты стихий (Огонь/Вода/Воздух) — ✅ ЗАВЕРШЕНА (10.09.2026)
1. `GameConfig.elements`: `burnDuration/burnRadius/spreadChance/maxIgnitePerDeath`, `slowFactor/wetDuration`, `pushStrength` ✅
2. `fluidProtocol`: оверлеи `water/airX/airY`, `set_effects`, `waterAt/airAt`, `FluidParams.waterSlowFactor/airPushStrength` ✅
3. `FluidWorker`: замедление + отброс в `integrate`; `FluidSimulationController.setEffects` ✅
4. `effectsCore` (чистое ядро: статусы + цепной поджог с шансом/лимитом) + `ElementEffectSystem` (зоны, refcount-оверлеи, тик статусов, tint) ✅
5. `GameScene`: интеграция (`applyElementStroke`, тик ~10 Гц, fallback-паритет), серая текстура + дефолтный красный tint, монстры поверх стихий (depth 850) ✅
6. Поп-ап «Длительность стихий» и «Длительность статуса» (общие ручки, конфиг per-element) + персист (TuningStore) ✅
7. jest (`effectsCore`/`fluidProtocol`), tsc/build/headless-смоук ✅

### Бэклог
- Комбинации стихий (Огонь+Вода=Пар и т.д.) и катализатор (молния)
- Тушение огня водой (статус Мокрый × Горение)
- Прокачка `gainPerKill/capacity/costPerUse/radius` в геймплее

---

## ⚠️ 8. Риски и открытые вопросы

1. **Развод жестов** — порог слайда уже есть (`controls.touch.swipeThreshold`), но на мобильных возможны ложные срабатывания; проверить плейтестом.
2. **Земля в воркере** — динамическая модификация коллизионной сетки и события атак требуют аккуратного расширения протокола; гонки с кадровым буфером (как с `arrived`).
3. **Стоимость за штрих vs за длину** — решено: мана списывается **по длине в реальном времени** (юнит `elementStrokeUnit`, цена `costPerUse` за юнит), линия наносится сразу, при исчерпании маны — обрыв на границе юнита. Неполный хвост доплачивается (`ceil`).
4. **Мана за боевые убийства** — супер сила тоже наполняет алтари (это осознанно); при прокачке может понадобиться разное поведение.

---

## 🔗 9. Ссылки

- [GAME_DESIGN_DOCUMENT.md](./GAME_DESIGN_DOCUMENT.md) — §2.2 (система стихий), §2.4 (алтари), §4 (план фаз)
- [GOD_POWER.md](./GOD_POWER.md) — главная стихия (не трогаем), паттерн прогресс-бара
- [FLUID_SIMULATION.md](./FLUID_SIMULATION.md) — воркер физики (расширение для Земли)
- Код: `src/game/element/*` (`ElementManaSystem`, `ElementDrawer`, `ElementAltarIcon`, `EarthBarrierSystem`, `earthCells`), `src/game/save/TuningStore.ts`, `src/game/fluid/*`, интеграция — `src/scenes/GameScene.ts`

---

**Дата обновления:** 10.09.2026
**Версия документа:** 1.0 (фича завершена)
